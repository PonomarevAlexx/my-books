export type Status = {
    LOADING: "loading";
    RESOLVED: "resolved";
    REJECTED: "rejected";
};

export type Author = {
    name: string;
    _id: string;
};

export type BookSeries = {
    name: string;
    _id: string;
};